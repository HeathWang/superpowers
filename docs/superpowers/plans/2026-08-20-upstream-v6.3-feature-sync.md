# Upstream v6.3 Feature Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the approved SDD dependency, independent-fix, and Devin/Hermes/Gemini feature batches from upstream v6.3.0.

**Architecture:** Copy authoritative, independent upstream files byte-for-byte where no fork policy is involved. Apply narrow integration edits only where an upstream feature intersects fork identity or an explicitly preserved policy.

**Tech Stack:** Markdown skills, Bash, Python, JavaScript, JSON, YAML.

**Testing:** Standard — use repository-local tests only.

**Spec:** none — requirements: merge groups 1, 2, and 3 approved on 2026-08-20; preserve OpenCode injection, narrowed TDD/verification triggers, and fork `executing-plans`; do not use `superpowers-evals`.

## Global Constraints

- Official source is `/Users/hb/Documents/open source/superpowers` at `b36e082` (v6.3.0).
- Preserve the fork repository URLs and current package version unless a feature requires a new manifest entry.
- Do not modify `.opencode/plugins/superpowers.js`, `skills/verification-before-completion/SKILL.md`, or `skills/executing-plans/SKILL.md`.
- Preserve the fork's explicit-only TDD trigger while replacing the test-quality reference.
- Do not stage, commit, push, or use `superpowers-evals`.

---

### Task 1: Close SDD Cross-Skill Dependencies

**Files:**
- Modify: `skills/requesting-code-review/code-reviewer.md`
- Modify: `skills/finishing-a-development-branch/SKILL.md`
- Modify: `skills/using-superpowers/references/codex-tools.md`
- Create/track: `skills/subagent-driven-development/re-review-prompt.md`
- Modify: `tests/claude-code/test-helpers.sh`
- Modify: `tests/claude-code/run-skill-tests.sh`
- Verify/preserve: `tests/claude-code/test-subagent-driven-development.sh`

**Interfaces:**
- Consumes: upstream SDD final-review and fix-round contracts.
- Produces: non-recursive reviewers, safe worktree cleanup, Codex event-driven waits/resume, and aligned Claude Code test helpers.

**Testing:** Standard

- [ ] Copy the authoritative dependency files from upstream; retain the already-identical `re-review-prompt.md` and the Fork's newer v6 task-review-shape behavior test.
- [ ] Run `bash tests/claude-code/test-sdd-workspace.sh`.
- [ ] Run `for file in tests/claude-code/test-helpers.sh tests/claude-code/run-skill-tests.sh tests/claude-code/test-subagent-driven-development.sh; do bash -n "$file"; done`.
- [ ] Read all Task files and verify the SDD lifecycle and worktree safety contracts match upstream.

### Task 2: Merge Independent Safety and Portability Fixes

**Files:**
- Modify: `hooks/hooks.json`
- Modify: `scripts/package-codex-plugin.sh`
- Modify: `tests/codex/test-package-codex-plugin.sh`
- Modify: `skills/systematic-debugging/find-polluter.sh`
- Create: `tests/systematic-debugging/test-find-polluter.sh`
- Modify: `skills/writing-skills/render-graphs.js`
- Create: `tests/writing-skills/test-render-graphs.sh`
- Create: `skills/test-driven-development/writing-good-tests.md`
- Delete: `skills/test-driven-development/testing-anti-patterns.md`
- Modify: `skills/test-driven-development/SKILL.md`
- Modify: `tests/hooks/test-session-start.sh`

**Interfaces:**
- Consumes: upstream v6.2/v6.3 bug-fix implementations.
- Produces: Git Bash SessionStart, GNU/bsd tar portability, correct polluter matching, Windows Graphviz probing, and stronger test-quality guidance.

**Testing:** Standard

- [ ] Copy upstream implementation and regression-test files.
- [ ] Replace only the fork TDD skill's test-quality reference section with the upstream `writing-good-tests.md` reference, preserving its explicit-only trigger.
- [ ] Run hook, Codex package, find-polluter, and render-graphs local tests.
- [ ] Run shell and JavaScript syntax checks.
- [ ] Read all Task files and verify the preserved TDD trigger and four independent fixes.

### Task 3: Add Devin, Hermes, and Gemini Support

**Files:**
- Create: `.devin-plugin/plugin.json`
- Create: `.hermes-plugin/__init__.py`
- Create: `.hermes-plugin/plugin.yaml`
- Create: `skills/using-superpowers/references/hermes-tools.md`
- Create: `skills/using-superpowers/references/gemini-tools.md`
- Modify: `skills/using-superpowers/SKILL.md`
- Modify: `skills/writing-skills/SKILL.md`
- Modify: `.version-bump.json`
- Modify: `scripts/bump-version.sh`
- Modify: `scripts/sync-to-codex-plugin.sh`
- Modify: `.gitignore`
- Create: `tests/devin/test-devin-plugin.sh`
- Create: `tests/hermes/__init__.py`
- Create: `tests/hermes/conftest.py`
- Create: `tests/hermes/test_bootstrap.py`
- Create: `tests/hermes/test_plugin.py`
- Create: `tests/version-bump/test-bump-version.sh`
- Modify: `README.md`

**Interfaces:**
- Consumes: native Devin skill discovery and Hermes `pre_llm_call`/`register_skill` APIs.
- Produces: installable manifests, first-turn Hermes bootstrap, tool mappings, version automation, Codex-sync exclusion, and installation guidance.

**Testing:** Standard

- [ ] Copy the upstream harness implementations, mappings, tests, and version-bump implementation.
- [ ] Set new manifest versions to the fork's current `package.json` version and replace upstream repository URLs in manifests, Hermes runtime errors, and installation documentation with the Fork URL.
- [ ] Add only the Hermes platform-adaptation line to `using-superpowers/SKILL.md`.
- [ ] Add only the upstream Gemini/Codex runtime-path sentence to `writing-skills/SKILL.md`.
- [ ] Add Devin/Hermes README sections using fork repository URLs.
- [ ] Run Devin, Hermes, and version-bump local tests.
- [ ] Read all Task files and verify both harnesses load stock skills and Gemini mapping exists.

### Task 4: Integrated Verification

**Files:**
- Verify: every file changed by Tasks 1–3.

**Interfaces:**
- Consumes: all three completed batches.
- Produces: a clean local verification report and a bounded final diff.

**Testing:** Standard

- [ ] Run `git diff --check`.
- [ ] Run local relevant test suites; do not invoke `evals/` or external repositories.
- [ ] Compare authoritative-copy files against upstream.
- [ ] Confirm preserved strategy files remain unchanged.
- [ ] Read the final changed-file list and verify no unrelated fork customization was overwritten.
